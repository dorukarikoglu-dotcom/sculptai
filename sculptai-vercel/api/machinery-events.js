import { createClient } from '@supabase/supabase-js'
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({ok:false});const url=process.env.SUPABASE_URL||process.env.VITE_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_ANON_KEY||process.env.VITE_SUPABASE_ANON_KEY;if(!url||!key)return res.status(200).json({ok:true,persisted:false,events:[]});try{const s=createClient(url,key,{auth:{persistSession:false}}),r=await s.from('machinery_events').select('*').order('ts',{ascending:false}).limit(500);if(r.error)throw r.error;return res.status(200).json({ok:true,persisted:true,events:r.data||[]})}catch(e){console.error(e?.message||e);return res.status(200).json({ok:true,persisted:false,events:[],warning:'supabase_read_failed'})}}
